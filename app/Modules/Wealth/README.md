# Wealth module

Optional NRTH module (disabled by default). Toggle under **Settings → Features**.

This directory holds the module’s PHP boundary (`Http`, `Providers`, `routes`). Inertia pages for the placeholder live at `resources/js/Pages/Wealth/` until a fuller frontend layout is needed.

Domain work (net worth, investments, providers) will land here in a follow-up.
